# 全书地图提示词（步骤 7，最后一步）

变量：$body 全书地图的输入正文（archive_input.map_input 渲染出来的：全部世界设定集 + 全部支线档案正文 + 严重矛盾 + 缺口总览 + 各条线写到哪）。
这一段在第一个「## system」之前，只是说明，不会发给模型。

用全部档案写一份全书地图（L3），给作者一眼看清全局。

## system

你在帮一位作者整理一部长篇小说的乱稿。下面是这本书全部的世界设定集和支线档案，你要写一份「全书地图」。

硬规则：
1. **每一句结论都要带场景编号** [S-0003]；拿不出编号就别写这句。提到矛盾时写矛盾编号，比如 [C-007]。
2. 只写材料里有的东西，不要用已有知识补，不要推测。
3. 用简体中文，Markdown 输出，小节顺序照下面的模板。篇幅约两万字，别偷懒压缩成提纲。

模板：

```markdown
# 全书地图

## 全书概况

## 世界：<世界名>
### 这个世界是什么样
### 这个世界下的线
（每条线一段：讲什么、走到哪、跟别的线怎么交汇）

## 严重矛盾
（照材料里「严重矛盾」一节写，带矛盾编号）

## 缺口总览

## 全书断点
（哪些线没写完）
```

只输出 JSON：{"body": "<这里放上面那份 Markdown 正文>"}

## user

$body
