# 已知问题与待办

计划①执行时各轮代码审查提出、当时决定暂不修的点。做到对应计划时顺手处理。

## 计划③（界面）要处理的

- **导入**：两个不同位置、同名的文件夹（比如 `D:\我的稿子` 和 `E:\我的稿子`）会共用 `原稿/我的稿子/`，后导入的覆盖前面的，并算作「改动」。导入界面要显示并记录来源文件夹路径，路径不同时提醒。
- **查重**：两个都被作者手选过主版本的组，重跑后合并成一组时，两个手选都会作废、退回自动选，界面上没有提示。
- **查重**：同一个文件中间插入一块内容，后面的场景编号会整体后移，作者手选的主版本会悄悄退回自动选。建议查重结果里记下「作废的手选」，界面上提醒。
- **查重**：查重任务正在运行时作者点「设为主版本」，有极小概率被查重结果覆盖。任务运行时禁用这个按钮即可。
- **API**：某个场景文件被改坏时，`GET /scenes` 只返回 500，没把出错的文件名告诉界面。
- **实体合并**：跨批冲突组里的非锚点名会成 single（安全，但漏合）。验收实测：觀音系 3 个叫法合进了一个实体，但「觀世音」单独成了 single；紅孩兒／牛聖嬰／善財童子没合。界面要把 conflicts 列出来给作者手动处理。
- **API**：卡文件结构被手改坏（`dropped` 里某项是 null、`card` 是字符串）时，卡片列表接口整体 500；实体.json 不是合法 JSON 时 `GET /entities` 500、confirm 等操作 400 且 detail 是原始 JSONDecodeError；实体条目缺字段的 KeyError 也会被报成「没有这个实体」404。跟上面那条同类：程序自己写的文件不会这样，只影响手改/外来文件，界面要给出具体出错信息而不是裸 500。

## 计划②b 要处理的

- **过期判定**：`book.py` 只把状态为 done 的步骤标过期；下游步骤运行中作者改了实体规范名，下游跑完仍是 done，用的却是旧规范名。如果②b 的档案按实体编号或场景列表组织，只看规范名映射来判过期是不够的。

## 已知局限（暂不处理）

- **导入**：Windows 上只改大小写的重命名（`a.txt` → `A.txt`）会被当成新文件，旧条目留着。和「源文件删了不从书里删」属于同一类。
- **查重**：同一小段文字有两份彼此相似的副本，又分别完整出现在两个不相干的章节里时，这四块仍会串成一组。需要凑巧的条件比较多。
- **切场景**：只有标题的块往后合并时，中间的分隔符行（`***`）会被包进块里。只影响显示，不丢内容。
- **场景**：`原稿/` 里的副本被手动删掉后，切场景会失败（报错带路径）。重新导入一次就会补回来。
- **验收脚本**：查重验收报告里没带导入失败 / 跳过的文件清单。
- **建书**：书名恰好是 Windows 保留名（`NUL`、`CON` 等）时，会报系统错误而不是友好提示。
- **实体合并**：`_context` 在名字与原文只差标点/全角时取不到上下文；同名跨类型（同一叫法既是人物又是地点）没处理；新出现的叫法即使实际指向某个已确认组，也不会自动并进去，会单独成 single，不提示它可能属于哪个已确认实体，要作者手动合并。
- **作者操作**：merge 每次要读全部场景卡，200 万字的书大约要 1 秒；实体文件的锁是进程内的，只支持单进程服务。
- **配置**：`PUT /api/config` 整份替换，前端必须提交完整表单；配置的读改写没加锁。
- **用量**：`add_usage` 不校验 step 名。
- **暂停**：这是设计如此——任务被暂停后步骤记为 failed，错误信息「已暂停：…」，重跑会接着做。
- **场景卡提示词**：pov 留空、quote 不许拼接两条已改（验收之后按作者意见改的，还没用真模型重跑验证，下次真跑看重试次数有没有降）；summary 要求简体但约 22% 的卡写成了繁体（未改）。
- **场景卡**（验收抽查 10 张发现，未改）：
  - 模型偶尔把名字写成简体（如「御弟師父」写成「御弟师父」），核对不做繁简转换，3 次重试后连名字带以它为 subject 的 facts 一起删掉——抽查里有一张卡因此丢了唐僧被俘经过的 4 条事实，人物名单里也没了唐僧。
  - 同一场景里同一人物换了称呼（「妖精」后面点明是「二魔」）会被列成两个人物，靠步骤 5 实体合并兜底。
  - incomplete 偶有误判：章回体「且听下回分解」的正常结尾被标成 incomplete=true，incomplete_note 自己却写着「情节并未中断」。
- **API**（代码质量审查 Minor，未改）：
  - 暂停接口对 import/split/dedup 也有效，暂停文案「做完的部分已经保存，重跑会接着做」对导入不准确（导入的清单最后才写），重跑本身没问题。
- **用量**：进程被杀（或断电）时，正在跑的这次调用不会计进 `book.json` 的用量统计（`cards.py:400-402`、`entities.py:525-527` 在 `finally` 里记账，进程被杀不会执行到）。卡和实体的缓存已经落盘，重跑不会重复花钱，只是累计费用会比实际少算一点。
- **场景卡暂停后单卡重做的 summary 会盖掉「已暂停」**：场景卡步骤被暂停后，步骤状态是 failed、summary.error 是「已暂停：…」；这之后如果单独重做某一张卡，步骤状态仍保持 failed（`only` 模式不改状态），但 summary 会被换成这一张卡的统计（`written`/`with_problems` 等，没有 `error` 字段）——作者看到的是一个「失败」的步骤配一张看起来全部成功的 summary，「已暂停」的原始信息没了。计划③做界面时要处理：`only` 模式下如果传入前的状态不是 done，就保留旧的 summary/error，不要用单卡这次的统计覆盖它。
